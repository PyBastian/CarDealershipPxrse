import { Save } from "lucide-react";
import { getSettings } from "@/lib/data";
import { saveSettings } from "./actions";
export default async function SettingsPage({ searchParams }: { searchParams: Promise<{ guardado?: string }> }) { const [settings, params] = await Promise.all([getSettings(), searchParams]); return <div className="admin-page form-page"><div className="admin-heading"><div><span className="eyebrow">Sitio</span><h1>Configuración</h1></div></div>{params.guardado && <p className="success-banner">Cambios actualizados.</p>}<form action={saveSettings} className="vehicle-form"><section><h2>Marca y contacto</h2><div className="form-grid"><label>Nombre de marca<input required name="brandName" defaultValue={settings.brandName}/></label><label>WhatsApp <small>Solo dígitos, con código de país</small><input required name="whatsappNumber" defaultValue={settings.whatsappNumber}/></label><label>Teléfono<input name="phone" defaultValue={settings.phone ?? ""}/></label><label>Correo<input type="email" name="email" defaultValue={settings.email ?? ""}/></label><label>Instagram<input type="url" name="instagramUrl" defaultValue={settings.instagramUrl ?? ""}/></label><label>Ubicación pública<input name="locationText" defaultValue={settings.locationText ?? ""}/></label></div></section><section><h2>SEO</h2><label>Título del sitio<input required name="siteTitle" defaultValue={settings.siteTitle}/></label><label>Descripción<textarea required name="siteDescription" defaultValue={settings.siteDescription}/></label><label className="inline-check"><input type="checkbox" name="showSold" defaultChecked={settings.showSold}/> Mostrar vendidos en el catálogo</label></section><button className="button"><Save/> Guardar configuración</button></form></div>; }
